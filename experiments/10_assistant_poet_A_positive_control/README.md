# Poet-A positive control

This notebook produced the 2026-08-06 positive-control run. The raw Inspect logs
are under `logs/`; checksums, configuration, token counts, and the validity note
are in [`RUN.md`](RUN.md).

The notebook is retained as the contemporaneous analysis record. Use
[`sandbox/runner.py`](../../sandbox/runner.py), rather than this historical
notebook, to run the current deduplicated methodology.
